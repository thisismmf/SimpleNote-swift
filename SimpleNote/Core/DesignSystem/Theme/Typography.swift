import SwiftUI

struct Typography {
    let h1 = Font.system(size: 24, weight: .bold)
    let h2 = Font.system(size: 20, weight: .bold)
    let body = Font.system(size: 16, weight: .regular)
    let caption = Font.system(size: 12, weight: .regular)
}
